name: DEPRECATED - Deploy to EC2

# ⚠️ THIS WORKFLOW IS DEPRECATED
# Deployment is now handled in django-ci.yml
# Kept for reference only - will be removed in future

on:
  workflow_dispatch:  # Manual trigger only
    
jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: |
          echo "::warning::This workflow is deprecated"
          echo "::warning::Use 'Django Production CI' workflow instead"
          echo "::warning::This file will be removed in a future update"